{
  "kind": "collectionType",
  "collectionName": "sessions",
  "info": {
    "singularName": "session",
    "pluralName": "sessions",
    "displayName": "Session",
    "description": "A scheduled instance of a Tour or Training"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {
    "i18n": {
      "localized": true
    }
  },
  "attributes": {
    "sessionName": {
      "type": "string",
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      }
    },
    "startDateTime": {
      "type": "datetime",
      "required": true
    },
    "endDateTime": {
      "type": "datetime",
      "required": true
    },
    "sessionStatus": {
      "type": "enumeration",
      "default": "scheduled",
      "enum": [
        "scheduled",
        "cancelled",
        "draft"
      ]
    },
    "sessionType": {
      "type": "enumeration",
      "default": "public",
      "enum": [
        "public",
        "private"
      ]
    },
    "capacity": {
      "type": "integer",
      "required": true
    },
    "trip": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::trip.trip",
      "inversedBy": "sessions"
    },
    "tour": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::tour.tour",
      "inversedBy": "sessionName"
    },
    "courses_collection": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::courses-collection.courses-collection",
      "inversedBy": "sessionName"
    },
    "training": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::training.training",
      "inversedBy": "sessions"
    }
  }
}
